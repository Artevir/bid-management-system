# 招标文件智能审阅中枢功能优化记录-20260415-010解析流水线章节树与专用化表写入

## 1. 本次目标

- 对照 `010.招标文件智能审阅中枢核心对象模型与主表逻辑总表-20260403-V1.0.md` 中「原文结构 / 招标要求资产 / 风险与规则 / 治理留痕」边界，补齐解析批次的**真实落库**，避免仅有 `tender_requirement` / `risk_item` 而缺少同域配套表与溯源链。
- 以可执行代码为主，不把篇幅花在过程性总装文档上；本记录仅固化结果与验证口径。

## 2. 010 对齐的开发计划（按域、可逐项勾选）

下列顺序与 010 的对象域一致，实施时以「schema 已存在 → 解析/任务写入 → API 暴露 → UI 可操作」为节奏。

### 2.1 项目与文档主控域（表 1–6）

- [x] 解析入口写入 `document_page`、`source_segment`（既有）。
- [x] 解析完成后回写 `source_document` 解析状态与页数（既有）。
- [x] 项目级 `parse_status` 聚合（既有）。

### 2.2 结构与框架识别域（表 7–10）

- [x] 每次首解析一份源文件时，写入 `document_section_node`：**卷级根节点** + 前 60 条分片对应的 **clause 子节点**，并挂 `source_segment_id`、起止页码。
- [ ] `bid_framework_node` 由规则/人工或独立任务生成（非纯文本启发式一步完成）。
- [ ] `framework_requirement_binding`、`attachment_requirement_node`：待框架与附件策略接入。

### 2.3 招标要求资产域（表 11–17）

- [x] `tender_requirement` 写入时补齐 `source_section_id`（指向上节 clause 节点）。
- [x] 按 `requirement_type` 同步写入专门化表：`qualification_requirement`、`commercial_requirement`、`technical_requirement`；`submission` 类写入 `submission_requirement`（版本 + 分片维度）。
- [x] 启发式写入 `time_node`、`money_term`（按分片去重，避免同段重复）。

### 2.4 风险与规则域（表 18–22）

- [x] `risk_item`（既有）。
- [x] `confidence_assessment`：以 `source_document` 为 target，记录抽取/业务置信度及 `reason_json`，`generated_by_batch_id` 指向解析批次。
- [ ] `rule_definition` / `rule_hit_record`：待规则引擎与批次任务对接（与 010 第四域一致）。

### 2.5 评分与技术域（表 23–26）

- [ ] `scoring_scheme` / `scoring_item` / `technical_spec_group` / `technical_spec_item`：待专用抽取任务或结构化表格管线。

### 2.6 模板与变量域（表 27–31）

- [ ] 模板主数据与变量绑定：依赖模板中心与变量提取任务。

### 2.7 材料与任务域（表 32–34）

- [ ] `submission_material` / `response_task_item` / `clarification_candidate`：待要求确认与任务编排策略。

### 2.8 AI 与治理域（表 35–38）

- [x] `object_change_log`：解析成功单文档后，以 `tender_project_version` 为 target 写入 `hub_ingestion_document_parsed` 摘要（便于与 `change-logs` API 联查）。
- [ ] `ai_task_run` / `review_task`：与模型调用、人工复核台深度绑定，本轮未扩展。
- [ ] `asset_export_snapshot`：与导出中心流程绑定，既有能力按需核对。

## 3. 本次代码改动

- 修改 `src/lib/tender-center/ingestion.ts`
  - 解析成功后：根章节树 + 分片级 clause 节点；`tender_requirement.source_section_id` 绑定。
  - 按类型批量插入资格/商务/技术/递交专门化行；时间与金额条款启发式入库。
  - 每份成功解析的源文件写入一条 `confidence_assessment`；每份写入一条版本维度的 `object_change_log`。
  - `IngestResult` 增加计数字段，便于 `parse` 路由与链路追踪消费。

## 4. 验证结果

- `pnpm exec tsc --noEmit`：通过。

## 5. 结果说明

- 本轮把 010 中「要求主表 + 专门化表 + 章节树 + 置信度 + 变更留痕」在**中枢解析流水线**上连成可验收的数据链；规则命中、评分技术、框架绑定等仍按上表留作后续迭代，避免一次改动面过大。
