# 招标文件智能审阅中枢功能优化记录-20260415-TC3-W8-003-示例数据可用性校验与联调引用门禁

## 1. 本次目标

- 落地 `TC3-W8-003`：围绕 `TC3-W8-002` 三类示例项目（完整/冲突/补跑）建立自动化可用性校验与联调引用门禁。
- 提供可复制的一键初始化 README 片段，并将门禁接入 CI（PR 必过）。

## 2. 本次代码改动

### 2.1 新增 W8-003 自动校验脚本（可 CI 阻断）

- 新增 `scripts/check-v3-w8-sample-projects.py`
  - 从 `src/db/seed.ts` 自动提取三类场景定义（`complete/conflict/replay`）。
  - 校验 `README.md` 是否包含 `TC3-W8-003` 一键初始化片段标记（START/END）。
  - 强制检查 README 片段包含：
    - `pnpm db:init`
    - `pnpm run docs:v3-w8-sample-projects:check`
    - `/api/tender-center/projects?keyword=TC3-W8-002`
    - 三个固定 `projectCode`
  - `--write`：更新快照并生成附录 J。
  - `--check`：发生快照漂移或 README 缺失即返回非 0。

### 2.2 新增快照与自动报告

- 新增 `contracts/tender-center-w8-sample-projects.json`
  - 固化三类示例项目的可机校验快照（状态、进度、parseError 标志等）。
- 新增 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0-附录J-W8-003-三类示例项目可用性与联调引用门禁.md`
  - 自动输出场景清单、差异结果与门禁命令。

### 2.3 README 一键初始化片段与联调引用入口

- 更新 `README.md`
  - 新增 `TC3-W8-003` 标记片段，覆盖依赖安装、`db:init`、门禁检查、示例 `projectCode`、联调查询入口。

### 2.4 工程脚本与 CI 门禁接入

- 更新 `package.json`
  - 新增：
    - `docs:v3-w8-sample-projects:update`
    - `docs:v3-w8-sample-projects:check`
- 更新 `.github/workflows/ci-cd.yml`
  - 在 `lint` job 增加 `W8-003 三类示例项目可用性与联调引用门禁（CI 阻断）`。

### 2.5 主计划索引与状态同步

- 更新 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0.md`
  - 新增附录 J 链接与脚本再生成说明。
  - 版本历史新增 `V3.0.12`（W8-003 落地）。
- 更新 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0-附录B-TC3与附录A核对状态.md`
  - `TC3-W8-003` 状态更新为 `☑`（基于可机校验门禁完成）。

## 3. 验证结果

- `pnpm run docs:v3-w8-sample-projects:update`：通过
- `pnpm run docs:v3-w8-sample-projects:check`：通过
- `pnpm type-check`：通过
- `pnpm lint`：通过
- `pnpm test --run`：通过（7 文件，28 用例）
- `pnpm run docs:v3-w8-seed-dictionary:check`：通过
- `pnpm run docs:v5-list-states-guard:check`：通过

## 4. 结果说明

- 本次实现了示例数据“可复用、可引用、可阻断漂移”的自动化门禁闭环。
- 后续任何对三类示例场景定义或 README 初始化片段的破坏性改动，都会在本地/CI 被立即拦截。
