# 招标文件智能审阅中枢功能优化记录-20260415-TC3-W9-001-四层联调检查表自动化

## 1. 本次目标

- 落地 `TC3-W9-001`：实现四层联调（结构/服务/页面/业务链路）自动检查表。
- 满足“每层 >= 5 条硬断言，且全部通过”的机器可校验门禁。
- 将门禁接入 CI，保证后续变更可回归、可阻断、可追溯。

## 2. 本次代码改动

### 2.1 新增四层联调自动校验脚本

- 新增 `scripts/check-v3-w9-integration-checklist.py`
  - 自动构建四层断言：
    - 结构层（Schema/seed/合同快照/关键脚本/关键测试文件）
    - 服务层（主链路与治理动作关键 API method+path）
    - 页面层（主链页面与详情页入口）
    - 业务链路层（W8 三场景快照与 seed 业务标识）
  - 每层断言数均 >= 5，并输出层级通过结论（`layerOk`）。
  - 支持：
    - `--write`：生成快照 + 附录 K
    - `--check`：快照漂移或断言失败即非 0 返回（CI 阻断）

### 2.2 新增快照与自动报告

- 新增 `contracts/tender-center-w9-integration-checklist.json`
  - 固化四层断言清单、通过结果与层级统计。
- 新增 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0-附录K-W9-001-四层联调检查表自动化.md`
  - 自动输出汇总、分层断言明细、差异与执行命令。

### 2.3 工程脚本与 CI 门禁接入

- 更新 `package.json`
  - 新增：
    - `docs:v3-w9-integration-checklist:update`
    - `docs:v3-w9-integration-checklist:check`
- 更新 `.github/workflows/ci-cd.yml`
  - 在 lint job 增加 `W9-001 四层联调检查表自动化门禁（CI 阻断）`。

### 2.4 主计划状态同步

- 更新 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0.md`
  - 新增附录 K 链接与脚本说明。
  - 版本历史增加 `V3.0.13`（W9-001 落地）。
- 更新 `docs/招标文件智能审阅中枢开发计划-交付级别-20260415-V3.0-附录B-TC3与附录A核对状态.md`
  - `TC3-W9-001` 状态从 `□` 更新为 `☑`。

## 3. 验证结果

- `pnpm run docs:v3-w9-integration-checklist:update`：通过
- `pnpm run docs:v3-w9-integration-checklist:check`：通过
- `pnpm type-check`：通过
- `pnpm lint`：通过
- `pnpm test --run`：通过（7 文件，28 用例）
- `pnpm run docs:v3-w8-sample-projects:check`：通过
- `pnpm run docs:v3-w8-seed-dictionary:check`：通过
- `pnpm run docs:v5-list-states-guard:check`：通过

## 4. 结果说明

- 本次将四层联调检查从“人工描述”升级为“可执行门禁”，并实现了快照基线化与 CI 阻断化。
- 后续若主链路接口、页面入口、示例场景或业务关键标识发生破坏，将在本地/CI 被及时拦截。
